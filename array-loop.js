 var numbers = [22, 44, 34, 43, 35, 3543,];
 for (var i = 0; i < 7; i++){
     var elements = numbers[i]
     console.log(elements);
 }

var items = ['laptop', 'phone', 'snacks', 'glass', 'paper'];
for (var i = 0; i < 1; i++){
    var elements = items[i]
    console.log(elements);
}

var letters = ['a', 'b', 'c', 'd', 'e' ,'f'];
letters.push('g');
letters[0] = 'h';
for (var i = 0; i < letters.length; i++){
    var item = letters[i];
    console.log(item);
}
